{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 0 capability set for the main window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
