# Instrument Packs

Each subdirectory is an instrument: `instrument.json` plus the resources its
backend needs (weights, `.pd` patch, embeddings), presets, and artwork. No
executable code. See docs/upi-app-spec.md "One Extension, Many Instruments".

- `hello-sine` — Phase 0. Oscillator backend, no data.
